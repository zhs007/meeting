# 项目编码规则（agent 专用）

1. 文件命名与结构
- 所有 Go 文件使用小写字母，单词间用下划线分隔。
- 包目录结构应简洁明了，遵循业务逻辑分层。

2. 代码风格
- 使用 4 个空格缩进。
- 变量、函数、类型命名采用驼峰式（如: myVariable, DoSomething）。
- 常量使用全大写字母，单词间下划线分隔（如: MAX_SIZE）。
- 每个函数、结构体、接口需有注释，说明用途。

3. Go 语言最佳实践
- 错误处理必须规范，优先返回 error 并及时处理。
- 尽量使用标准库，第三方库需在 go.mod 中声明。
- 接口设计应简洁，避免过度抽象。

4. 测试与文档
- 每个业务逻辑需有对应单元测试，测试文件以 _test.go 结尾。
- 重要函数和模块需有详细注释和使用示例。

5. 代码提交与审查
- 每次提交需保证代码可编译、测试通过。
- 重要变更需经团队成员审查。

---
本规则仅供 agent 自动化处理和生成代码时参考。
